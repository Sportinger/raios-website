export const BOOT_SCENES = Object.freeze([
  Object.freeze({ id: "uefi-firmware", label: "UEFI Firmware" }),
  Object.freeze({ id: "boot-usb", label: "raiOS Boot USB" }),
  Object.freeze({ id: "limine-bridge", label: "Limine" }),
  Object.freeze({ id: "kernel-load", label: "Kernel laden" }),
  Object.freeze({ id: "boot-information", label: "Startinformationen" }),
  Object.freeze({ id: "control-handoff", label: "Kontrollwechsel" }),
  Object.freeze({ id: "kernel-landing", label: "Kernel landet" }),
]);
