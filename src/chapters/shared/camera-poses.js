export const BARE_METAL_DRIFT_POSE = Object.freeze({
  position: Object.freeze([13.28, 2.96, 9.39]),
  up: Object.freeze([0, 1, 0]),
});

export const BARE_METAL_IMPACT_POSE = Object.freeze({
  position: Object.freeze([12.1, 2.96, 9.4]),
  target: Object.freeze([0, -0.76, 0]),
  up: Object.freeze([0, 1, 0]),
});

export const BARE_METAL_BOOT_POSE = Object.freeze({
  position: Object.freeze([8.6, 5.3, 9.6]),
  target: Object.freeze([0, -0.55, 0]),
  up: Object.freeze([0, 1, 0]),
});
