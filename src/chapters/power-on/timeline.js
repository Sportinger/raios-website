export const POWER_ON_TIMELINE = Object.freeze({
  reveal: Object.freeze([0.02, 0.18]),
  press: Object.freeze([0.3, 0.47]),
  settle: Object.freeze([0.47, 0.58]),
  power: Object.freeze([0.42, 0.62]),
  exit: Object.freeze([0.72, 0.96]),
});
